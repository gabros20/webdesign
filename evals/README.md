# Webdesign evaluations

These portable fixtures separate activation, route selection, output quality, and compression
regressions. Model-driven runners should execute each activation and traversal prompt multiple
times and record selected files, token use, latency, and outcome assertions.
